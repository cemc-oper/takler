version: 2

sphinx:
  configuration: doc/source/conf.py

formats:
  - epub
  - pdf

build:
  os: "ubuntu-24.04"
  tools:
    python: "3.12"

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs